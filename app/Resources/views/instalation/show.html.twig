{% extends 'base.html.twig' %}

{% block body %}
    <h1>Instalation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instalation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ instalation.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('instalation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('instalation_edit', { 'id': instalation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
